apiVersion: 1

datasources:
  - name: Prometheus
    # The committed dashboard refers to its datasource as ${DS_PROMETHEUS};
    # this uid is what that resolves to.
    uid: DS_PROMETHEUS
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
